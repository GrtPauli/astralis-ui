import { CheckboxCard } from "./components/checkbox-card";

// 1️⃣ Named export (grouping comes from Checkbox.Group, like Checkbox itself)
export { CheckboxCard };

// 2️⃣ Type exports
export type { CheckboxCardProps } from "./checkbox-card.types";
