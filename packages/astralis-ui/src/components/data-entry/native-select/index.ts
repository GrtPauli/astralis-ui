import { NativeSelect } from "./components/native-select";

// 1️⃣ Named export (no sub-components — options are native elements)
export { NativeSelect };

// 2️⃣ Type exports
export type { NativeSelectProps, NativeSelectSize, NativeSelectVariant } from "./native-select.types";
