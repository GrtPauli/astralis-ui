import { Rating } from "./components/rating";

// 1️⃣ Named export (no sub-components for Rating)
export { Rating };

// 2️⃣ Type exports
export type { RatingProps, RatingSize } from "./rating.types";
