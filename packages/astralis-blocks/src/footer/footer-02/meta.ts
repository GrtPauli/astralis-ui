import type { BlockMeta } from "../../../registry";

export default {
  id: "footer-columns-02",
  category: "footer",
  family: "footer-columns",
  name: "Newsletter capture with three columns",
  description:
    "Five-column grid on the page surface: brand, blurb and an inline newsletter form take two columns, then three columns of links. The bottom row pairs the copyright with legal links rather than social icons.",
  tags: ["three-columns", "newsletter", "email-capture", "legal-row", "base-surface"],
} satisfies BlockMeta;
