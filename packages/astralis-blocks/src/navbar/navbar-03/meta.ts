import type { BlockMeta } from "../../../registry";

export default {
  id: "navbar-inline-03",
  category: "navbar",
  family: "navbar-inline",
  name: "Floating pill with centred links",
  description:
    "The same bar inset from the page edges as a rounded, bordered card rather than a full-bleed strip. Wordmark left, centred links, one call to action right, and a disclosure that opens inside the pill below md. Uses the raised surface, so it lifts in dark mode where a shadow alone would not read.",
  tags: ["sticky", "floating-pill", "centred-links", "single-action", "raised-surface"],
} satisfies BlockMeta;
