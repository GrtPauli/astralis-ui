import { selectProps } from "@/modules/demos/select/select-props";

export const metadata = {
  title: "Select",
  description: "A custom dropdown with search, groups, clearing, and full keyboard navigation.",
};

# Select

A custom dropdown with search, groups, clearing, and full keyboard navigation.

<ComponentPreview name="select-demo" />

## Import

```tsx
import { Select } from "astralis-ui";
```

## Usage

Options come in as data — flat `{ value, label }` items or
`{ group, options }` sections — and the dropdown portals out of any
`overflow: hidden` ancestor, so it works inside cards and modals.

## Searchable, clearable, grouped

<ComponentPreview name="select-searchable" />

## States

<ComponentPreview name="select-states" align="start" />

## Props

<PropsTable rows={selectProps} />

## Keyboard

| Key | Action |
| --- | --- |
| `Enter` / `Space` | Open; when open, select the highlighted option |
| `↓` | Open when closed; move the highlight down |
| `↑` | Move the highlight up |
| *type* (with `searchable`) | Filter the options |
| `Esc` | Close and refocus the trigger |
| `Tab` | Close and move on |

## Accessibility

- The trigger is a `combobox` with `aria-haspopup="listbox"` and
  `aria-expanded`; options carry `role="option"`.
- Selecting an option returns focus to the trigger.
