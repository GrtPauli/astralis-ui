import { sliderProps } from "@/modules/demos/slider/slider-props";

export const metadata = {
  title: "Slider",
  description: "Single and dual-thumb range controls with marks, tooltips and keyboard support.",
};

# Slider

Single and dual-thumb range controls with marks, tooltips and keyboard support.

<ComponentPreview name="slider-demo" />

## Import

```tsx
import { Slider, RangeSlider } from "astralis-ui";
```

## Marks

`marks` accepts `true` for auto-marks at every step, or an array with
optional labels for the ones worth naming.

<ComponentPreview name="slider-marks" />

## Range

`RangeSlider` is the dual-thumb sibling — same props, tuple value.

<ComponentPreview name="slider-range" />

## Props

<PropsTable rows={sliderProps} />

## Keyboard

| Key | Action |
| --- | --- |
| `→` / `↑` | Increase by `step` |
| `←` / `↓` | Decrease by `step` |
| `Home` / `End` | Jump to `min` / `max` |

## Accessibility

Thumbs render `role="slider"` with `aria-valuemin/max/now`; RangeSlider's
two thumbs are each independently focusable and clamp against each other.
