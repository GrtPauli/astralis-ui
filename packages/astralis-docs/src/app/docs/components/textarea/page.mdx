import { textareaProps } from "@/modules/demos/textarea/textarea-props";

export const metadata = {
  title: "Textarea",
  description: "A multi-line text field with variants, character count, and Field integration.",
};

# Textarea

A multi-line text field with variants, character count, and Field integration.

<ComponentPreview name="textarea-demo" />

## Import

```tsx
import { Textarea } from "astralis-ui";
// also available as Input.TextArea
```

## Props

<PropsTable rows={textareaProps} />

Inside a [Field](/docs/components/field), the label association, `required`,
and help/error `aria-describedby` wiring all come for free.
